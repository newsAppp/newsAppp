/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/newsappp.github.io",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
  

export default nextConfig;
